name: Send full commit history to Discord

on:
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Send commits in chunks
        run: |
          echo "Frontend update" | while read header; do
            RESPONSE=$(curl -s -o response.txt -w "%{http_code}" \
              -H "Content-Type: application/json" \
              -X POST \
              -d "$(jq -n --arg text "$header" '{content: $text}')" \
              ${{ secrets.DISCORD_WEBHOOK }})

            echo "HTTP: $RESPONSE"
            cat response.txt
          done

          git log --reverse --pretty=format:"- %h: %s" | while IFS= read -r line; do
            RESPONSE=$(curl -s -o response.txt -w "%{http_code}" \
              -H "Content-Type: application/json" \
              -X POST \
              -d "$(jq -n --arg text "$line" '{content: $text}')" \
              ${{ secrets.DISCORD_WEBHOOK }})

            echo "HTTP: $RESPONSE"
            cat response.txt
          done
